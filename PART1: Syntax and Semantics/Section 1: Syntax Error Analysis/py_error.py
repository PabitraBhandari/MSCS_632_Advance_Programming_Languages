def calculate_sum(arr  # Missing closing parenthesis in function definition
    total = o  # 'o' is not defined; should be '0'
    for num in arr  # Missing colon at the end of the for loop
        total += num
    return total

numbers = [1, 2, 3, 4, 5]
result = calculate_sum (numbers)
print("Sum in Pyhton :", result)  # Typographical error in string
